# Unified Requirements for QR Live Detection
# Works on Desktop (Windows/Mac/Linux) and Raspberry Pi 5
# Install with: pip install -r requirements.txt

# Core dependencies
opencv-python>=4.8.0
numpy>=1.21.0
pandas>=1.3.0

# Optional: Enhanced OpenCV features (uncomment if needed)
# opencv-contrib-python>=4.8.0

# For data visualization and testing
matplotlib>=3.5.0

# Raspberry Pi specific dependencies (will be ignored on other platforms)
# Install separately on Pi 5: pip install picamera2
# picamera2>=0.3.12

# Development and testing
# qrcode[pil]>=7.4.2  # For generating test QR codes (optional)

# Note: Platform-specific installation notes:
# 
# Raspberry Pi 5:
#   sudo apt install python3-picamera2
#   pip install picamera2
#
# Windows:
#   May need Visual C++ Redistributable
#
# macOS: 
#   brew install opencv (if pip install fails)
#
# Linux (Ubuntu/Debian):
#   sudo apt install python3-opencv (alternative to pip)
#   sudo apt install libhdf5-dev libhdf5-serial-dev
#   sudo apt install libqtgui4 libqtwebkit4 libqt4-test python3-pyqt5
#   sudo apt install libatlas-base-dev libjasper-dev