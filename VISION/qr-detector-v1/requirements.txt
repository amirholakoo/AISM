# Python dependencies for QR Live Detection
# Note: PiCamera2 is installed via apt on Raspberry Pi OS; see README.md

numpy 1.24.3
opencv-python
pandas

# Optional (Raspberry Pi camera stack)
# On Raspberry Pi OS, prefer system packages:
#   sudo apt update && sudo apt install -y python3-picamera2
# If you must use pip (may not work on all Pi setups), try at your own risk:
# picamera2
